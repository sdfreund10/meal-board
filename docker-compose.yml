services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mealboard
      POSTGRES_PASSWORD: mealboard
      POSTGRES_DB: mealboard
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mealboard -d mealboard"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
